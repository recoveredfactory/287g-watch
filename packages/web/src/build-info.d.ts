// Injected by Vite `define` (see vite.config.ts): the build timestamp as an
// ISO string, frozen at build time and rendered in the site footer. Ambient
// global — no import/export here, or it stops being global.
declare const __BUILD_TIME__: string;
